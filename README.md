# denmont.dev
Personal Portfolio.

## Local preview

From the project directory, run:

```sh
python3 -m http.server 8000
```

Then open `http://localhost:8000/`. This preserves the same clean URLs used by the deployed site.
